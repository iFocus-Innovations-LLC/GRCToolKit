---
# OWASP GenAI LLM Top 10 2026 — LLM06:2026 Unbounded Consumption (read-only)
# https://genai.owasp.org/llm-top-10/
# HITL: no auto-remediation. Part of GRCToolKit Shields Up.
#
# Usage:
#   cd ansible/playbooks/llm
#   ansible-playbook -i inventory.yml llm06-unbounded-consumption.yml

- name: "OWASP LLM06:2026 — Unbounded Consumption — read-only validation"
  hosts: all
  gather_facts: yes
  vars_files:
    - vars/defaults.yml
  vars:
    llm_findings: []
    llm_report_basename: "llm06-unbounded-consumption"
  tasks:
    - name: "Include common setup"
      include_tasks: tasks/setup.yml
      tags: always

    - name: "Include LLM06:2026 probes"
      include_tasks:
        file: tasks/llm06-unbounded-consumption.yml
        apply:
          tags: [LLM06, unbounded_consumption]
      tags: [LLM06, unbounded_consumption]

    - name: "Include report"
      include_tasks: tasks/report.yml
      tags: always
