apiVersion: v1
kind: Service
metadata:
  name: grc-toolkit-service
  namespace: grc-toolkit
  labels:
    app: grc-toolkit
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: grc-toolkit
