apiVersion: v1
kind: ConfigMap
metadata:
  name: grc-toolkit-config
  namespace: grc-toolkit
data:
  # Environment-specific configurations can be added here
  APP_NAME: "GRC Toolkit"
  APP_VERSION: "2.1.0-dev"
  APP_BUILD_DATE: "2026-02-06"
  FEATURE_PQC: "enabled"
  # Add any other configuration values as needed
